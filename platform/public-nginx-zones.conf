# Shared, bounded traffic state for WYWA public-life sites.
limit_req_zone $binary_remote_addr zone=wywa_public_per_ip:10m rate=60r/m;
limit_conn_zone $binary_remote_addr zone=wywa_public_conn_per_ip:10m;
